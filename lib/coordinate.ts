export default class Coordinate {
  constructor(public x: number, public y: number) {}

  distanceFrom(other: Coordinate) {
    // TODO: actually calculate distance between the coordinates.
    //       e.g. return Math.abs(this.x - other.x);

    return 0
  }
}
